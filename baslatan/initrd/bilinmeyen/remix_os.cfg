if [ -s (loop)/isolinux/remixos.png ]; then
	set title="Remix OS"
	linux (loop)/kernel iso-scan/filename=$isofile androidboot.selinux=permissive quiet SERIAL=random logo.showlogo=1 SRC= DATA= DATA_IN_MEM=1 
	initrd (loop)/initrd.img
fi